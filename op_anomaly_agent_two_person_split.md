# 双人协作版：经营异动诊断 Agent —— 任务拆分 + Agent 自我迭代设计（v2：按流水线纵切，而非数据/Agent横切）

> 修订说明：上一版按"数据层 vs Agent层"横切，会导致一个人整天调 SQL、另一个人整天调 Prompt，各自只对半个系统有真实的设计经验，简历上也只能各写各的半段故事。这版改成**按 LangGraph 的流水线阶段纵切**——每人各自负责的那段流程里，从工具函数、Prompt 设计、到"这个阈值该怎么定"这种产品判断，全部自己拿主意，两人都是完整意义上的"Agent 开发者"，不是一个人管数据、一个人管 Agent。

---

## 1. 分工总览：按流水线纵切

整个 Agent 是一条四步流水线：**确认异常 → 下钻定位 → 知识检索 → 归因生成/反思/出报告**。把它从中间切一刀,分成"前半段（感知与定位）"和"后半段（推理与产品化）"，两人各包一段，各自对自己那段的**工具、Prompt、设计决策**全权负责：

- **队友A：感知与下钻段** —— 负责"确认异常""下钻定位"这两个节点，从数据怎么查、Prompt 怎么写、"跌多少算异动"这种阈值怎么定，全流程自己设计
- **队友B：推理与产品段** —— 负责"知识检索""归因生成/反思/出报告"这两个节点，从知识库怎么建、Prompt 怎么写、报告长什么样，全流程自己设计

两人交接点只有一处：队友A 那段最后输出"下钻结果"（一个标准格式的 dict），队友B 那段从这个 dict 开始接着往下走。这个格式在 Phase 0 就锁死，之后各自关起门来开发,互不阻塞。

---

## 2. 详细任务分配表

| 阶段 | 队友A（感知与下钻段） | 队友B（推理与产品段） | 两人共同 |
|---|---|---|---|
| Phase 0：架构共设 | —— | —— | 一起画 LangGraph 流程图，一起定四个节点的输入输出格式（尤其是 A→B 交接的那个 dict 长什么样），一起讨论"什么算显著异动""报告要包含哪些字段"这些产品问题——**架构和产品决策必须两人一起做，不能谁分到哪段就只懂哪段** |
| Phase 1：各自开发（并行） | 1. 选数据集/建 DuckDB 表<br>2. 写 `query_metric_data`、`calculate_change`、`drilldown_by_dimension` 三个工具<br>3. 写"确认异常"和"下钻"两个节点的 Prompt（用 LangGPT 框架）<br>4. 自己决定异常阈值、下钻维度顺序这些参数，并能说出为什么这么定 | 1. 写 metric_dictionary.md、troubleshooting_playbook.md，搭 Chroma + `search_knowledge` 工具<br>2. 写"知识检索"和"归因生成/反思/报告"三个节点的 Prompt<br>3. 设计结构化报告的 JSON Schema（诊断结论/归因路径/建议动作长什么样）<br>4. 先出一版报告的 UI 草图（不用写代码，纸面设计即可） | 每天 15 分钟同步进度，互相看一眼对方的 Prompt 写法，提前发现"两人风格差太多"的问题（比如一个用中文变量名一个用英文，尽早统一） |
| Phase 2：拼图联调 | —— | —— | 一起把两段接起来跑通整个 LangGraph，一起 debug（这是两人对整个系统建立完整认知的关键环节，不能一人主导另一人旁观） |
| Phase 3：评估与迭代 | 针对"确认异常/下钻"这两个节点构造 5-8 条评估案例，自己迭代自己的 Prompt | 针对"知识检索/归因生成"这两个节点构造 5-8 条评估案例，自己迭代自己的 Prompt | 每周一次互相 review 对方的 bad case 和改进方案，两人对彼此的节点都要能看懂、能提修改意见——这是简历上"跨模块协作迭代"真正的依据 |
| Phase 4：产品化 | 参与 Streamlit 界面里"异常确认/下钻展示"部分的实现（比如涨跌幅卡片、下钻柱状图） | 参与 Streamlit 界面里"报告展示"部分的实现（比如诊断报告卡片、建议动作文字区） | 一起做整体页面布局和交互流程设计，两人对完整产品形态都要有贡献，不是一人包圆前端 |
| Phase 5：收尾 | 讲清楚"感知与下钻段"的设计逻辑：为什么按品类/地区/渠道这个顺序下钻、阈值怎么定的 | 讲清楚"推理与产品段"的设计逻辑：知识库怎么构建的、报告格式为什么这么设计 | 合写复盘文档和简历素材，互相对彼此那段也要能讲出个大概，面试被问到对方负责的部分不能一问三不知 |

---

## 3. Agent 迭代 / 自我迭代机制设计

这部分逻辑不变，但责任人跟着纵切逻辑走：**每人先迭代自己那段的 Prompt，再互相 review**，而不是一人管数据一人管 Prompt。

### 第一层：评估驱动的迭代（必做）

1. 两人各自为自己负责的节点构造小份评估集（A 负责"下钻定位准不准"，B 负责"归因理由靠不靠谱"），合并成一份完整的 `eval_set.json`。
2. 各自写评估脚本，跑出自己那段的命中率。
3. 各自拿着自己的 bad case，去 Kerry 课上教的**提示词优化器**（`prompt-optimizer-six-sepia.vercel.app`）或找 Claude/Monica 扮演"Prompt 诊断专家"分析问题、给改进建议——这一步两人都做，不是只有一个人用这个工具。
4. 改完后重新跑评估集，存版本记录（`node_a_prompt_v1/v2/v3`、`node_b_prompt_v1/v2/v3`），两份迭代曲线都要能拿出来，简历上两人各自都有真实数字可写。
5. 每周互相 review 一次对方的迭代记录，提出"如果是我我会怎么改"的建议——这一步才是"协作迭代"真正发生的地方。

### 第二层：长期记忆自我改进（选做）

人工确认对的诊断案例存回 Chroma，作为未来相似案例的参考。这一层因为跨了 A/B 两段的数据（下钻结果 + 归因结论都要存），天然需要两人一起设计存储格式，不会出现"只有一人管"的情况。

### 第三层：PromptOptimizerAgent（进阶/加分项）

可以做成两个独立的小型优化 Agent（各自负责建议 A 段/B 段的 Prompt 修改），也可以做成一个通用的、能读懂整条流水线的优化 Agent——如果两人都想挑战一下，后者更能体现"设计一个跨模块的自我迭代机制"这种更高阶的系统思维，值得作为最后冲刺目标。

---

## 4. 会用到的工具/平台清单（两人都用，不再按角色区分）

| 工具/平台 | 用途 | 使用方式 |
|---|---|---|
| **DuckDB** | 结构化业务数据存储与 SQL 查询 | 队友A 主建表结构，但两人都要会写查询 |
| **ChromaDB** | 向量知识库 | 队友B 主建索引，但两人都要懂怎么往里加案例（长期记忆机制需要） |
| **Claude API** | 主力推理模型 | 两人各自的节点都用 |
| **DeepSeek API** | 批量评估时的低成本模型 | 两人各自跑自己节点的评估集时都用，省成本 |
| **提示词优化器**（Kerry 课上的工具） | 辅助分析 bad case、生成改进版 Prompt | 两人各自迭代自己节点时都用 |
| **Monica** | 快速在无代码平台验证一版 Prompt 逻辑，通过了再搬进代码 | Phase 0 架构共设阶段，两人一起用它快速试验几种 Prompt 写法 |
| **GitHub** | 版本控制、任务看板 | 两人共用，Phase 1 各自开在自己的分支上，Phase 2 合并 |
| **Streamlit + Streamlit Community Cloud** | 演示界面与部署 | 两人各自实现自己负责的 UI 区域，一起联调 |

---

## 5. 压缩后的时间线（约 13-15 天）

- Day 1-2：Phase 0 架构共设（两人一起）
- Day 3-6：Phase 1 各自开发（并行，各自的工具+Prompt+设计决策）
- Day 7-8：Phase 2 拼图联调（两人一起）
- Day 9-11：Phase 3 评估与迭代（各自迭代+互相 review）
- Day 12-13：Phase 4 产品化（各自实现+一起设计）
- Day 14-15：Phase 5 收尾（部署、复盘、简历素材）

这版拆分比"数据 vs Agent"横切多花一点协调成本（Phase 0 的架构共设、Phase 2 的一起联调都需要两人同时在场），但换来的是**两人都能完整讲出"我设计了一个 Agent 节点，从工具到 Prompt 到产品判断都是我做的"**，这在面试里比"我负责数据、他负责 Agent"这种回答含金量高得多。
